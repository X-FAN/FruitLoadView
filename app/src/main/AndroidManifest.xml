<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xf.fruitloadview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RedAppleActivity" />
        <activity android:name=".BlueAppleActivity" />
        <activity android:name=".LemonActivity" />
        <activity android:name=".WatermelonActivity" />
        <activity android:name=".AllActivity"></activity>
    </application>

</manifest>